use pyo3_build_config::add_extension_module_link_args;

fn main() {
    // This is just a placeholder to allow building the crate as a library.
    // The actual functionality is exposed through the Python module.
    add_extension_module_link_args();
}
